[project]
name = "fakeLogNovel"
version = "1.0.0"
description = "假日志生成器：将源文本按模板格式拆分成多份日志文件"
requires-python = ">=3.8"

[project.scripts]
fakenovel = "main:run_gui"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"